<button type="submit" class="btn btn-primary mt-2" disabled="@(!IsValid)">@ChildContent</button>

@code {

    [Parameter]
    public bool IsValid { private get; set; } = true;

    [Parameter]
    public RenderFragment? ChildContent { get; set; }

}